{% extends 'AdminBundle::layout.html.twig' %}
{% block pagetitle %}
	<h1>{{ phrase('admin.tickets.delete_priority') }}</h1>
	<nav>
		<a class="clean-white" href="{{ path('admin_ticketpris') }}">&larr; {{ phrase('admin.tickets.back_to_ticket_priorities') }}</a>
	</nav>
{% endblock %}
{% block page %}

<p>{{ phrase('admin.tickets.confirm_delete_priority', {'title':priority.title}) }}</p>

<p>{{ phrase('admin.tickets.notice_priority_will_be_removed') }}</p>

<a class="btn" href="{{ path('admin_ticketpris_dodel', {'priority_id': priority.id, 'security_token': security_token('delete_ticket_priority')}) }}">{{ phrase('admin.general.confirm_delete') }}</a>

{% endblock %}
