{% extends 'AdminBundle:CustomDefAbstract:edit.html.twig' %}
{% import 'AdminBundle:Common:design-macros.html.twig' as design %}
{% block pagebar %}
     <ul>
		<li><a href="{{ path('admin_customdeforganizations') }}">{{ phrase('admin.general.organization_fields') }}</a></li>
		{% if field.id %}
			<li>{{ phrase('admin.general.edit_field') }}</li>
		{% else %}
			<li>{{ phrase('admin.general.new_field') }}</li>
		{% endif %}
     </ul>

	{% if field.id %}
		<nav>
			<ul>
				<li class="delete"><a href="{{ path('admin_customdeforganizations_delete', {'field_id': field.id, 'security_token': security_token('delete_custom_field') }) }}" onclick="return confirm('Are you sure?');">{{ phrase('agent.general.delete') }}</a></li>
			</ul>
		</nav>
	{% endif %}
{% endblock %}
