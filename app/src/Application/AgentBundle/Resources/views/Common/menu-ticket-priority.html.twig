<ul class="menu priority_id {{add_classname}}" data-option-name="priority_id" style="display:none">
	{% for id,title in priorities %}
		<li class="pri-{{id}}" data-option-id="{{id}}">{{title}}</li>
	{% endfor %}
</ul>