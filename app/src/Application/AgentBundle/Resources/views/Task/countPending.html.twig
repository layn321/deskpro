{
  "person" : {
    "total" : {{ tasks.person.total }},
    "due_today" : {{ tasks.person.due_today }},
    "overdue" : {{ tasks.person.overdue }}
  },
  "teams" : {
    "total" : {{ tasks.teams.total }},
    "due_today" : {{ tasks.teams.due_today }},
    "overdue" : {{ tasks.teams.overdue }}
  },
  "delegated" : {
    "total" : {{ tasks.delegated.total }},
    "due_today" : {{ tasks.delegated.due_today }},
    "overdue" : {{ tasks.delegated.overdue }}
  },
  "all" : {
    "total" : {{ tasks.all.total }},
    "due_today" : {{ tasks.all.due_today }},
    "overdue" : {{ tasks.all.overdue }}
  }
}